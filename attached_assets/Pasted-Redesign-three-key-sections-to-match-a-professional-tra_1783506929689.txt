Redesign three key sections to match a professional trading journal UI. 
Reference these specific features:

---

## 1. ANALYTICS PAGE — full redesign

Add time period filter bar at the top:
- Buttons: Today | 7 Days | 30 Days | 3 Months | 1 Year | All Time
- Active button: solid blue #3b82f6 background
- Filter by: All Trades | Winners | Losers (right side)

Top stat cards (4 cards in a row):
- TOTAL P&L — value in green/red, subtext "From X closed trades"
- WIN RATE — value, subtext "X wins • X losses" with progress bar
- PROFIT FACTOR — value, subtext "Excellent/Good/Poor" based on value
- EXPECTANCY — value, subtext "Average per trade"

Quick Stats grid below (2x4 grid of smaller cards):
- Avg Winner | Avg Loser
- Best Trade | Worst Trade  
- Win Streak | Loss Streak
- Risk:Reward | Open Trades

Equity Curve chart:
- Toggle between Equity and Drawdown views
- Clean line chart, no fill, white/blue line on dark background

All data must respect the selected time period filter.

---

## 2. TRADE ANALYSIS PAGE — new feature under Analytics

Add "Trade Analysis" as a sub-item under Analytics in sidebar.

Layout: split view — trade list on left, trade detail on right.

LEFT PANEL — trade list:
- Each trade card: symbol + direction badge (LONG=green, SHORT=red)
  entry price + P&L + date/time
- NEW badge on recent trades (added in last 7 days)
- Filter tabs: All | Winners | Losers
- Filter by: All Time dropdown + Sort By dropdown

RIGHT PANEL — trade detail (when trade is clicked):
- Header: symbol large, WINNER/LOSER badge, Score badge, P&L large
- Direction + date + duration
- 4 stat cards: Entry Price | Exit Price | Quantity | Price Move %
- Trade Simulation section:
  - Candlestick chart showing the trade entry to exit
  - Timeframe buttons: 1m | 5m
  - Playback controls: Play | Reset | Reload Data
  - Speed selector: 0.1x | 0.25x | 0.5x | 1x | 2x | 4x | 8x
  - Timeline slider showing entry and exit markers
  - Use lightweight-charts library for the candlestick chart
  - Fetch OHLCV data from a free API (use Twelve Data or Alpha Vantage 
    free tier for XAUUSD/forex data)

---

## 3. AI REPORT PAGE — redesign history list

Replace raw JSON history with a clean report list:

Each past report card shows:
- Report title (generated by AI as a headline e.g. 
  "Close to Profitability, Fix the Leaks")
- Date range + generated date
- P&L for that period in green/red
- Win rate + trade count badges
- Status tag: Profitable / Break Even / Loss

Header section:
- Show countdown timer to next available report 
  e.g. "⏱ 1d 7h 59m — Resets Friday"
- "Generate Report" button (greyed out if limit reached)
- "0/2 reports used this week" counter

When user clicks a past report → open it in a full rendered view
(NOT raw JSON — parse and render as cards as instructed before)

Store a generated title for each report in the DB when it is created.
Ask the AI to return a "title" field in the JSON response.

---

## GENERAL RULES:
- Keep existing dark theme
- All new components must match existing card style (dark bg, subtle borders)
- No raw JSON ever visible to users
- All filters must be functional, not just visual